rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Media uploads are not wired to an authenticated, moderated workflow yet.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
