"use client";

import AppShell from '@/features/layout/AppShell';

/**
 * PRODUCTION ROOT (VAINAKH v2.0)
 * The main page is now a lean orchestrator delegating to specialized features.
 */
export default function Home() {
  return <AppShell />;
}
